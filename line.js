class Line{
		constructor(returning, vertical, constPos, rot){
				this.returning = returning;
				this.vertical = vertical;
				this.constPos = constPos;
				this.rot = rot;
				this.cars = [];
		}
}
